<div class="wrapper">

  <div class="chat-room">
    <div class="chat-room__header">
      <nav class="chat-room__nav-left">
        <%= link_to :back do %>
        <i class="fas fa-chevron-left"></i>
        <% end %>
        <%= @chat_room_user.name %>
      </nav>
      <nav class="chat-room__nav-right">
        <% if @chat_room_user.profile_image? %>
        <%= image_tag @chat_room_user.profile_image.url ,class: 'talk-user-img' %>
        <% else %>
        <div class="user-default-img"></div>
        <% end %>
      </nav>
    </div>

    <div id='chat-messages'>
      <%= render @chat_messages %>
    </div>

    <div class="chat-room__form" name="form1">
      <%= form_with model: @message do |f| %>
      <%= f.text_area :content, :placeholder => "メッセージを入力して下さい" ,class: "chat-room__message-form_textarea" %>
      <%= f.hidden_field :chat_room_id, :value => @chat_room.id %>
      <label>
        <%= f.submit class: "" %> <i class=" fas fa-paper-plane chat_btn none-btn"></i>
      </label>
      <% end %>
    </div>
    <div class="form-bottom"></div>
    <div> <%= paginate @chat_messages %></div>
  </div>

</div>